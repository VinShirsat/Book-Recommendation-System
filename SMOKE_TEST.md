Run a quick smoke test locally after installing requirements:

python src/run.py --method hybrid --user_id 1 --book_id 1

This should print combined recommendations from assoc, collab and content modules.
